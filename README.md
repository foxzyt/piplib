# piplib
Python tool that automatically scans your python files and installs all dependencies

How to setup:

1. Clone the repository using Git in your desired folder
2. Add that folder to your PATH (global or user PATH, dosen't really matters)

That's it!

How to use it:

1. Go to your folder where your Python files are
2. Execute: piplib install 
